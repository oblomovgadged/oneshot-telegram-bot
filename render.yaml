services:
  - type: web
    name: oneshot-telegram-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python backend/live_alarm_scanner.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
